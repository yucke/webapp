{
  "name": "webapp",
  "version": "1.0.0",
  "private": true,
  "description": "WOS webapp deployment scripts and Cloudflare Worker",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
